//post first successful output
// input: data corpus
// output: a flattend Lookup and counts (cumsum of the bin size)
#include <iostream>
#include <fstream>
#include <vector>
#include <stdlib.h>     /* calloc, exit, free */
#include <numeric>
#include <algorithm>
#include <string> 


using namespace std;

void Index(float* data, uint32_t* Lookup, uint32_t* counts, string method){
    //LSH
    //kmeans
    //BLISS
    //IVF : using its internal search function
}

